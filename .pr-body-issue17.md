## Audit/fix for Issue #17

This is an audit/fix, not a redesign. Every rendered claim now traces to owner-confirmed SiteConfig or is omitted.

### What was audited
- **Statistics**: Removed hardcoded stats (15+ years, 12K boats moved, 26 states) from primitives showcase. Only renders DOT stat when site.dotNumber is confirmed.
- **DOT references**: Footer DOT column already conditional on hasDot; primitives page now uses site.dotNumber instead of "DOT-12345" placeholder.
- **Insurance language**: All "Licensed & insured" claims across Hero trust strip, Footer brand column, all metadata (layout/home/about), OpenGraph descriptions, and JSON-LD structured data are now conditional on `site.dotNumber && site.insuranceStatement`.
- **Credentials/claims**: Removed unconfirmed "$2M general liability" specific claim from FAQ insurance answer in primitives showcase.
- **Phone numbers**: Replaced all 3 hardcoded phone numbers in CoverageSection with site.phone from SiteConfig (was the only confirmed value).

### TrustBar verification
- TrustBar correctly renders nothing when no confirmed stats (all pages pass empty CONFIRMED_STATS array) ✅
- Footer conditional rendering verified: DOT column shows only when hasDot, insurance statement shows only when hasInsurance ✅

### Verification
- TypeScript: clean (`tsc --noEmit`)
- Next.js build: passes (14/14 pages compiled successfully)
- No hardcoded claims remain in production code
