## Objective
Implement the service-area and photo-proof sections for home (reused on /coverage and /gallery).

## Changes

### New Components
- **CoverageSection** (`src/components/sections/CoverageSection.tsx`)
  - Semantic region list using `<ul role="list">` with state cards (1-up mobile → 2-up tablet)
  - Static SVG map of East Coast service states — no JS library
  - Compact variant for use on /coverage page
  - TODO(owner-content) markers respected; unconfirmed areas flagged
- **GalleryStrip** (`src/components/sections/GalleryStrip.tsx`)
  - Responsive image grid (1-up mobile → 2-up tablet → 3-up desktop)
  - Uses ImageFrame with explicit dimensions (640×480) and lazy loading
  - Empty state: placeholder frames matching Hero's grid-pattern treatment
  - Caption + category metadata per card

### Updated Pages
- **Home page** — replaced TODO placeholders with CoverageSection + GalleryStrip
- **/coverage** — full coverage treatment with map + region list
- **/gallery** — full photo grid treatment

## Acceptance Criteria
- [x] Coverage renders at mobile/tablet/desktop without breakage; semantic list/SVG, no JS
- [x] Gallery images lazy-load below fold, have explicit dimensions and meaningful alt text
- [x] No stock clichés — empty state uses restrained placeholder frames
- [x] Keyboard pass clean; visual consistency with tokens
- [x] TypeScript strict: zero errors
- [x] next build: clean (zero warnings)

## Validation
- `tsc --noEmit` ✓
- `next build` ✓
- Responsive at 320px, 768px, 1280px breakpoints

## Dependencies
- #3 — Content boundary types & getters (already merged)
- #7 — ServiceGrid & ProcessSteps sections (already merged)
