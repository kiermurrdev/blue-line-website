import type { Testimonial } from "@/types/content";

/**
 * Testimonials — real ones only (BRAND_ASSETS_CHECKLIST §B).
 * Section ships empty until owner supplies them.
 */
export const testimonials: Testimonial[] = [];
