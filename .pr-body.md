## Objective
Build the transport gallery per the approved sitemap — photo proof of real jobs.

## What Changed

### New Components
- **`GalleryCard`** — Card component with placeholder state (no stock imagery), explicit image dimensions, lazy loading below fold, meaningful alt text, and caption area
- **`CategoryFilter`** — Client-side category filter tabs with blue-line active indicator (the "blue line" motif as a functional device)

### Updated Components
- **`Badge`** — Added `variant="blue"` and `size="sm"` for category tag styling

### Gallery Page (`/gallery`)
- Responsive grid: 1-up mobile / 2-up tablet / 3-up desktop
- Category filter tabs (shown when categories exist in content)
- CTA link to `/contact` when items exist
- Empty state with intentional placeholder treatment — clearly marked, no stock imagery

### Content
- `gallery.ts` — Placeholder entries with `TODO(owner-content)` markers, no real images yet

## Acceptance Criteria

- [x] Grid renders at mobile/tablet/desktop (responsive columns) without breakage
- [x] Every image has explicit dimensions, lazy loading below fold, meaningful alt text; captions present on every item
- [x] Single H1; metadata correct; CTA reachable
- [x] No stock clichés; placeholder state clearly marked `TODO(owner-content)`

## Validation
- ✅ `next build` passes cleanly (no errors)
- ✅ TypeScript compiles without errors
- ✅ Responsive grid: 1-col → 2-col → 3-col via Tailwind breakpoints
- ✅ Placeholder treatment uses blue-line motif SVG, not stock imagery
- ✅ All images use `fill` with explicit `sizes` attribute for responsive loading

## Screenshots
_(Will add once owner-supplied photos land and visual review is possible)_

Refs: #16
