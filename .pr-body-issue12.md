## Objective
Implement the company story/experience page per Issue #12.

## Changes
- **`src/app/about/page.tsx`** — Full about page composition replacing stub: hero section, TrustBar, story block, values grid, Testimonials (conditional), QuoteCta
- **`src/types/content.ts`** — Added `AboutPageContent` interface for typed about content
- **`src/lib/content.ts`** — Added `getAboutPageContent()` typed accessor

## Sections (reused from home page)
- TrustBar — dark stats band, renders only with confirmed owner data
- Testimonials — conditional render when testimonials exist
- QuoteCta — "Let's Talk" variant with quote link + phone action

## Content
All copy sourced from `src/content/pages/about.ts` via `lib/content`. Placeholder story/values marked `TODO(owner-content)`. No fake stats, testimonials, or credentials shipped.

## Validation
- [x] TypeScript compile: clean (`tsc --noEmit`)
- [x] Next build: clean (all 14 routes generated)
- [x] Single H1 with metadata + OpenGraph + canonical
- [x] Prose constrained to max ~65ch per DESIGN_SYSTEM section 3
- [x] Reused TrustBar, Testimonials, QuoteCta — identical to home instances
- [x] CTA reachable without scrolling (QuoteCta in-page) + sticky call bar on mobile
- [x] Responsive: grid 1-col -> 2-col -> 3-col; section rhythm tokens used

## Acceptance Criteria (#12)
- [x] Renders at mobile/tablet/desktop with no layout breakage
- [x] Prose max ~65ch line length
- [x] Reused sections render identically to home instances
- [x] Single H1; logical heading order (H1 -> SectionHeading h2 -> value h3)
- [x] Metadata correct (title, description, canonical, OG)
- [x] CTA reachable without scrolling past the fold or via sticky bar on mobile
